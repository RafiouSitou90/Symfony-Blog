{% extends 'base.html.twig' %}

{% block title %}Edit account{% endblock %}

{% block body %}
    <h1>Edit account</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

        <button type="submit">Update</button>
    {{ form_end(form) }}
{% endblock %}

